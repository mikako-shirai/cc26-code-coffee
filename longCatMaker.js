const longCatMaker = (n) => {
  const prefix = 'Long cat is l';
  const suffix = 'ng!';
  let os = '';

  for (let i = 0; i < n; i++) {
    os += 'o';
  }
  return prefix + os + suffix;
};


// Long Cat Maker
// Secret Code: BA67C33C
